/*!
 * 点/边的双连通分量
 * 最简图描述
 * #define N 100
 * typedef struct _Edge {
 *  int m_vertex; //邻接点索引值
 *  int m_st; //描述状态，可省略
 *  struct _Edge *m_next; //下一条边
 * } Edge;
 * Edge graph[N];
 * char vertex[N];
 * unsigned char visted[N]; //0:undefined 1:discovered 2:discovered
 * vertex描述定点, visted描述访问状态
 */
#include "common.h"

int
main(int argc, char* argv[]) {
    return 0;
}
