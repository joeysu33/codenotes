/* 
**  mod_foo.c -- Apache sample foo module
**  [Autogenerated via ``apxs -n foo -g'']
**
**  To play with this sample module first compile it into a
**  DSO file and install it into Apache's modules directory 
**  by running:
**
**    $ apxs -c -i mod_foo.c
**
**  Then activate it in Apache's httpd.conf file for instance
**  for the URL /foo in as follows:
**
**    #   httpd.conf
**    LoadModule foo_module modules/mod_foo.so
**    <Location /foo>
**    SetHandler foo
**    </Location>
**
**  Then after restarting Apache via
**
**    $ apachectl restart
**
**  you immediately can request the URL /foo and watch for the
**  output of this module. This can be achieved for instance via:
**
**    $ lynx -mime_header http://localhost/foo 
**
**  The output should be similar to the following one:
**
**    HTTP/1.1 200 OK
**    Date: Tue, 31 Mar 1998 14:42:22 GMT
**    Server: Apache/1.3.4 (Unix)
**    Connection: close
**    Content-Type: text/html
**  
**    The sample page from mod_foo.c
*/ 

#include "httpd.h"
#include "http_config.h"
#include "http_protocol.h"
#include "ap_config.h"

/* The sample content handler */
static int foo_handler(request_rec *r)
{
    if (strcmp(r->handler, "foo")) {
        return DECLINED;
    }
    r->content_type = "text/html";      

    if (!r->header_only)
        ap_rputs("The sample page from mod_foo.c\n", r);
    ap_rputs("<h1> The real httpd extension examples </h1>", r);
    return OK;
}

static void foo_register_hooks(apr_pool_t *p)
{
    ap_hook_handler(foo_handler, NULL, NULL, APR_HOOK_MIDDLE);
}

/* Dispatch list for API hooks */
module AP_MODULE_DECLARE_DATA foo_module = {
    STANDARD20_MODULE_STUFF, 
    NULL,                  /* create per-dir    config structures */
    NULL,                  /* merge  per-dir    config structures */
    NULL,                  /* create per-server config structures */
    NULL,                  /* merge  per-server config structures */
    NULL,                  /* table of config file commands       */
    foo_register_hooks  /* register hooks                      */
};

