/*!
 象棋残局，其中红方有n(2<<n<<7)个棋子，黑方只有一个将。红方除了有一个帅(G)之外，还有3中可能的棋子
 ：车(R), 马(H), 炮(C),并且要考虑
1.蹩马腿，
2.将和帅不能在一条直线上
输入所有棋子的位置，保证局面合法并且红方已经将军。
你的任务是判断红方是否已经把黑方将死。
*/
#include <iostream>

using namespace std;

int main()
{
    return 0;
}
